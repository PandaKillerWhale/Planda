const express = require('express');
const router = express.Router();

// router.get('/notebook/cards/:notebookId');

// router.delete('/notebook/:notebookId')
// router.put('/notebook/:notebookId')

module.exports = router;
